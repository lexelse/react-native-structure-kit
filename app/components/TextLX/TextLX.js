// @flow
import React from "react";
import { Text } from "react-native";

const TextLX = (props) => {
    const { children } = props;

    return (
        <Text>{children}</Text>
    );
};

export default TextLX;
